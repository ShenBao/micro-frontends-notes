{
  "name": "micro-app-demo",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "build": "npm-run-all --parallel build:main build:micro build:micro1",
    "build:main": "cd main-app && npm run build",
    "build:micro": "cd micro-app && npm run build",
    "build:micro1": "cd micro-app-1 && npm run build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "koa": "^3.0.3",
    "koa-send": "^5.0.1",
    "koa-static": "^5.0.0"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
